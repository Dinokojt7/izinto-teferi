import 'package:flutter/material.dart';

class AppInfoPopUp extends StatelessWidget {
  const AppInfoPopUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
